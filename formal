#!/bin/sh
set -eu

# Resolve the true path of this script, following symlinks.
SCRIPT_PATH="$(readlink -f "$0" 2>/dev/null || realpath "$0" 2>/dev/null || echo "$0")"
SCRIPT_DIR="$(cd "$(dirname "${SCRIPT_PATH}")" && pwd)"

exec python3 "${SCRIPT_DIR}/tools/formal.py" "$@"
